[Unit]
Description=OBSOLETE: ares-omokoda-memory — Julia memory/compute service (archived; superseded by Rust)
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/ares/Omo-Koda2/omokoda-memory
# Replaced by Rust service — see omokoda-core/src/server.rs
# ExecStart=/usr/local/bin/julia --project=. server.jl --port 7778
ExecStart=/bin/true
Restart=no
User=root

[Install]
WantedBy=multi-user.target
