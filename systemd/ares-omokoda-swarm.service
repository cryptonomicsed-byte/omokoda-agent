[Unit]
Description=OBSOLETE: ares-omokoda-swarm — Elixir OTP supervision tree (archived; superseded by Rust)
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/ares/Omo-Koda2/omokoda-swarm
# Replaced by Rust service — see omokoda-core/src/server.rs
# ExecStart=/usr/bin/mix run --no-halt
ExecStart=/bin/true
Restart=no
User=root

[Install]
WantedBy=multi-user.target
